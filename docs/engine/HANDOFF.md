# Stellar Engine migration handoff

Clean engine 0.1.16 is committed as `0054aee40eb74a4cd05da8d44556dcd0dd02d7ef`. Its benchmark package is `Builds/Windows/StellarContinuum-windows-benchmark-0054aee4-20260913T061208458531Z`, with `sourceDirty: false`, 39/39 CTest and 19/19 Python checks, seven sealed runtime files, and successful relocated fresh-campaign validation. The exact export log is `work/native-016-clean.log`. Prior clean 0.1.15 evidence is recorded in Git history; native/build/Windows/research/voice CI passed while screenshots were still running at that observation. The 0.1.13 and 0.1.14 screenshot runs were cancelled, not passed.

Engine 0.1.17 integrates colony commands and timed establishment (88 forwarding cases, 36 actual C# runtime sequences, four native boundaries), strategic intent/provider replay, observer-local strategic planning (75 source cases, 54 commands), and read-only combat readiness/prototype capabilities (56+22 source cases). It also repairs settlement population formatting with two actual-source regressions. The maintained combined build passes 43/43 CTest and 19/19 Python checks, recorded in `work/native-034-strategic-input-support-testing.log`. See each subsystem's validation document for fixture hashes, source-only null observations and limitations. The next exact-commit export establishes the clean 0.1.17 package.

Continue without stopping at this checkpoint: strategic own-state construction (035), matched combat commands (036), and the retained strategic director/runtime (037) are in draft. Their boundaries are recorded in STRATEGIC_INPUT_RUNTIME_MIGRATION_CONTRACT.md and COMBAT_COMMAND_RUNTIME_MIGRATION_CONTRACT.md. Then compose the ordered full campaign coordinator with fresh borrowed views after ship/colony append. Do not count source fixture rows as executed parity without invoking the corresponding native operation; integer comparisons must be exact, and harness parsing must remain outside expected-operation exception capture. Full campaign scheduling, Adaptive Research, save-v16 persistence and SDL3/Vulkan presentation remain open.

PR #325 remains a draft against integration. No integration/main merge or Godot removal is authorized before full parity. The existing Godot 0.1.7 Alpha remains the playable baseline. Territorial PR #323 is paused with enclosed pockets unresolved. No native graphical, player-save, playable-runtime or FPS parity claim applies.
