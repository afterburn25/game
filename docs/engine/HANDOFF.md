# Stellar Engine migration handoff

Clean engine 0.1.17 is committed as `a0aed3258fb17a617cbe6ac92c690ec74ac9c01a`. Its benchmark package is `Builds/Windows/StellarContinuum-windows-benchmark-a0aed325-20260913T064352695360Z`, with `sourceDirty: false`, 43/43 CTest and 19/19 Python checks, seven sealed runtime files, and successful relocated launch and fresh-campaign validation. The exact export log is `work/native-017-clean.log`. All six GitHub workflows for that commit passed, including Game Screenshot Capture run 34743410624. Prior clean export evidence is recorded in Git history. The 0.1.13 and 0.1.14 screenshot runs were cancelled, not passed.

Engine 0.1.17 integrates colony commands and timed establishment (88 forwarding cases, 36 actual C# runtime sequences, four native boundaries), strategic intent/provider replay, observer-local strategic planning (75 source cases, 54 commands), and read-only combat readiness/prototype capabilities (56+22 source cases). It also repairs settlement population formatting with two actual-source regressions. See each subsystem's validation document for fixture hashes, source-only null observations and limitations.

Engine 0.1.18 now integrates strategic own-state construction (035), matched combat commands (036), the retained strategic director/runtime (037), ordered campaign stepping and combat outcomes (038), and exact-own fleet status (039). Combined Windows validation passed 48/48 CTest and 19/19 Python checks in `work/native-038-campaign-testing.log`. The final tightened campaign consumer also passed focused strict Debug and Release replays. These are development checks; the clean 0.1.18 export must be recorded after committing. The coordinator queries capabilities at their original point of use, retains one shared callable, and reconstructs borrowed views after ship/colony append. It preserves the legacy research path and does not yet migrate Adaptive Research.

Continue without stopping: finish remaining coordinator commands (040), promote the maintained multi-step campaign benchmark (041) after review, then port the immutable Adaptive Research catalog (042). Their current drafts are isolated from this checkpoint. Follow SOURCE_PARITY_ACCEPTANCE.md: actual native calls, exact integers, decoded source inputs, invocation-only error capture and complete post-failure state. ADAPTIVE_RESEARCH_MIGRATION_CONTRACT.md records the existing game's actual research/diplomacy composition and its distinct phase order. Adaptive Research, diplomacy, save-v16 persistence and SDL3/Vulkan presentation remain open.

PR #325 remains a draft against integration. No integration/main merge or Godot removal is authorized before full parity. The existing Godot 0.1.7 Alpha remains the playable baseline. Territorial PR #323 is paused with enclosed pockets unresolved. No native graphical, player-save, playable-runtime or FPS parity claim applies.
