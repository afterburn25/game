# Stellar Engine migration handoff

Clean engine 0.1.15 is committed as `c77d185e0e932bc2189dd4ee31ea84cf2e12ffd5`. Its benchmark package is `Builds/Windows/StellarContinuum-windows-benchmark-c77d185e-20260913T053859427176Z`, with `sourceDirty: false`, 37/37 CTest and 19/19 Python checks, seven sealed runtime files, and successful relocated fresh-campaign validation. The exact export log is `work/native-015-clean.log`. Prior clean evidence remains in the exploration, freight and settlement validation documents. For 0.1.15, native/build/Windows/research/voice CI passed; screenshots were still running at the latest observation. The 0.1.13 and 0.1.14 screenshot runs were cancelled, not passed.

Engine 0.1.16 integrates settlement opportunity planning (88 actual C# cases) and stateful combat (73 actual C# cases, 79 commands checked after every call). The maintained testing build passes 39/39 CTest and 19/19 Python checks. See SETTLEMENT_PLANNING_VALIDATION.md and COMBAT_SIMULATION_VALIDATION.md for exact fixtures and boundaries. The next exact-commit export establishes the clean package; do not label earlier pre-commit evidence as clean.

Next: verify the exact-commit 0.1.16 export while colony commands/establishment gate031 and strategic intent/planning gates032/033 finish actual-source parity. Gate031's initial fixture includes forwarded planner cases; do not count those as independent runtime coverage. Gate032's first consumer only counted provider rows and was rejected; its repaired consumer must execute every source-recorded provider operation. The colony, strategic planning and campaign coordinator contracts record source boundaries, mutation order and growable-vector lifetimes. Gate numbers identify work, not engine versions. Full campaign scheduling, matched combat commands, adaptive research and persistence remain open; do not claim partial advancement is a full game loop.

PR #325 remains a draft against integration. No integration/main merge or Godot removal is authorized before full parity. The existing Godot 0.1.7 Alpha remains the playable baseline. Territorial PR #323 is paused with enclosed pockets unresolved. No native graphical, player-save, playable-runtime or FPS parity claim applies.
